import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.pioloalba.ionicdailytaskmanager',
  appName: 'Daily Task Manager',
  webDir: 'dist'
}

export default config
